apiVersion: apps/v1
kind: Deployment
metadata:
  name: ljwx-website
  labels:
    app: ljwx-website
    component: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ljwx-website
  template:
    metadata:
      labels:
        app: ljwx-website
        component: web
    spec:
      imagePullSecrets:
        - name: ghcr-pull
        - name: regcred
      containers:
        - name: ljwx-website
          image: ghcr.io/brunogao/ljwx-website:latest
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
