apiVersion: v1
kind: Service
metadata:
  name: quality-dashboard
  labels:
    app: quality-dashboard
    component: monitoring
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: quality-dashboard
