apiVersion: v1
kind: Service
metadata:
  name: agent-platform
  labels:
    app: agent-platform
spec:
  selector:
    app: agent-platform
  ports:
    - name: http
      port: 8000
      targetPort: http
  type: ClusterIP
